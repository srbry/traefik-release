<%
  traefik_mode = p("mode")
  if traefik_mode != "web"
    raise ArgumentError, "Expected mode to be 'web' but was: '#{traefik_mode}'"
  end
%>
[<%= traefik_mode %>]

address = ":<%= p("port") %>"

readOnly = <%= p("read_only") %>
